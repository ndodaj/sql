/**
  The USE command in SQL is used to select a specific database within a database management system (DBMS).
  Once a database is selected, all subsequent SQL commands are executed within the context of that database.
 */

USE database_name;
