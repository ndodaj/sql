/**

  The RENAME command in SQL is used to rename database objects like tables, columns, or other schema objects.
  The syntax and support for RENAME vary depending on the database management system (DBMS) being used.
 */

ALTER TABLE PRIVATE.TENAGERS RENAME TO PRIVATE.RENAMED_TABLE;